rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // Hanya user login yang boleh read/write
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}
